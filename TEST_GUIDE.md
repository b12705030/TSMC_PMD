# PMS 測試指南

> 版本：2026-05-07  
> 涵蓋所有已實作功能的手動測試步驟，建議依照章節順序執行以確保測試資料依賴正確。

---

## 環境確認

```bash
# 後端
cd backend && npm run start:dev   # http://localhost:3001

# 前端
cd frontend && npm run dev        # http://localhost:3000

# 資料庫 migration 確認
cd backend && npx prisma migrate status
```

---

## 測試帳號

> 依照 seed 資料，帳號密碼請參考 `backend/prisma/seed.ts`。

| 角色 | 說明 | 用於測試 |
|------|------|----------|
| **Admin** | 全系統管理，可跨 Region | 建立跨 Region 週期、全局設定 |
| **RegionalHR** | 台灣區 HR | 建立/發布模板、推進週期 |
| **Manager** | 部門經理 | 校準排名、發布結果、回覆申訴 |
| **Supervisor** | 直屬主管（受 Manager 管理）| 填寫主管評核、查看下屬目標 |
| **Employee** | 一般員工（受 Supervisor 管理）| 填寫自評、提出申訴 |

---

## 快速 Checklist（UX 修正 #1–#11）

在完整測試前可先跑這份清單確認 UX 修正都生效：

| # | 測試方式 | 通過條件 |
|---|----------|----------|
| 1 | Sidebar 看文字 | 「我的評核」下有小字「自己的績效表單」；「團隊評核」下有「下屬的評核狀況」 |
| 2 | Supervisor 點未填評核 | 頁面頂部出現黃色 banner |
| 3 | 任一評核頁 | 頁面內容佔滿全寬，無 max-w-2xl 限制 |
| 4 | HR 新增 MultipleChoice 題目（模板詳情頁）| 題型選多選題後出現選項輸入區 |
| 5 | Supervisor 填寫中的評核 | 左右分欄出現（左欄員工自評、右欄主管意見） |
| 6 | Supervisor 選等第時 | ⓘ hover 出現 O→U 六等第定義 |
| 7 | 評核列表 badge | 未填寫的評核顯示「待員工自評」而非「待您填寫」 |
| 8 | 校準頁改排名 | blur 後數字留存，不整頁 reload |
| 9 | Employee 看已發布評核 | 出現「主管逐題評語」區塊 |
| 10 | Employee 端結果頁 | 等第旁顯示中文名稱與說明 |
| 11 | Manager 回覆申訴 | 表單頂部出現等第選擇器 |

---

## 第一章：帳號與身分驗證

### T-AUTH-01 登入

1. 開啟 `http://localhost:3000`，未登入應自動跳轉至 `/login`
2. 輸入錯誤密碼 → 應顯示錯誤訊息，不跳轉
3. 輸入正確帳密 → 跳轉至 Dashboard
4. **預期 Cookie**：瀏覽器 DevTools > Application > Cookies 應出現 `sessionId`

### T-AUTH-02 登出

1. 登入後點左下角「登出」
2. **預期**：跳回 `/login`，`sessionId` cookie 消失
3. 手動輸入 `http://localhost:3000/dashboard` → 應被導回 `/login`

### T-AUTH-03 角色隔離

| 操作 | 角色 | 預期 |
|------|------|------|
| 直接訪問 `/cycles`（管理週期）| Employee | 看不到「新增週期」按鈕 |
| 直接訪問 `/templates`（模板管理）| Employee | 看不到建立按鈕 |
| 直接呼叫 `POST /api/cycles`（用 curl）| 無 session | 回傳 401 |
| 直接呼叫 `PATCH /api/cycles/:id/advance` | Supervisor token | 回傳 403 |

---

## 第二章：績效週期管理

### 前置：以 RegionalHR 或 Admin 登入

### T-CYCLE-01 建立週期

1. 前往「週期管理」，點「+ 新增週期」
2. 填寫：
   - 名稱：`測試週期 2026 Q3`
   - 類型：Quarterly
   - 地區：Taiwan（RegionalHR 自動帶入；Admin 可多選）
   - 目標設定期：今天起算 7 天內
   - 評核期：目標設定期結束後 14 天
3. 點「建立週期」
4. **預期**：週期出現在列表，狀態為 `GoalSetting`（Stepper 第 2 格高亮）

### T-CYCLE-02 週期 Stepper 顯示

1. 查看已建立的週期卡片
2. **預期**：
   - 上軌道「模板準備」：HR 建立模板（未完成）→ 模板已發布（未完成）→ 經理補充問卷（未完成）→ ◆
   - 下軌道「評核流程」：公司/部門目標公布（✓）→ 目標設定（高亮）→ 後續格子灰色

### T-CYCLE-03 推進週期狀態機

> 此測試需先完成模板建立（見第三章）

每次推進前確認：
- 按鈕文字正確（例：`InProgress` 狀態的按鈕應顯示「開始員工自評 →」）
- 確認 Dialog 標題：`確認推進至下一階段？`
- 確認 Dialog 描述格式：`【週期名稱】將推進至：XXX。此操作無法復原。`（注意是【】而非""）
- 確認推進後無法後退

| 目前狀態 | 推進按鈕 | 推進後狀態 |
|----------|----------|------------|
| GoalSetting | 進入執行中 | InProgress |
| InProgress | 開始員工自評 | EmployeeReview |
| EmployeeReview | 開始主管初評 | SupervisorReview |
| SupervisorReview | 開始校準發布 | Calibration |
| Calibration | 完成週期 | Completed |

### T-CYCLE-04 推進至 EmployeeReview 前的擋關

1. 在 `InProgress` 階段，尚未建立任何模板時，點「開始員工自評」
2. **預期**：後端回傳 400，前端顯示錯誤（不推進）

### T-CYCLE-05 推進 Dialog 顯示未完成問卷的 Manager 名單

1. 週期處於 `InProgress`，部分 Manager 尚未新增自訂問題
2. 點「開始員工自評 →」
3. **預期**：確認 Dialog 出現後，下方黃色區塊列出尚未補充問卷的 Manager 姓名
4. 所有 Manager 都已完成時：顯示綠色「✓ 所有經理均已完成問卷補充。」

### T-CYCLE-06 Admin 編輯週期

1. 以 Admin 登入，找到 `GoalSetting` 狀態的週期
2. 點「編輯」
3. 修改名稱、地區、日期
4. **預期**：儲存後資料更新
5. 在非 `GoalSetting` 狀態嘗試編輯 → 不顯示「編輯」按鈕

### T-CYCLE-07 週期警告提示 icon

1. 週期處於 `InProgress`，模板未建立時
2. **預期**：顯示 SVG 三角形警告 icon（非彩色 emoji ⚠），文字顯示「▲ 尚未建立模板...」
3. Icon 顏色應跟著 `text-amber-600` 的顏色，無獨立顏色

---

## 第三章：表單模板管理

### 前置：以 RegionalHR 登入

### T-TMPL-01 建立模板（基礎題目）

1. 前往「評核模板」，點「+ 新增模板」
2. 填寫：
   - 模板名稱：`工程師 L2-L4 Q3 評核`
   - 績效週期：選擇上方建立的週期
   - 適用職等：L2、L3、L4
   - 適用職稱：Process Engineer、Software Engineer
3. 新增三道基礎題：
   - 題目：「本季最重要的成就是什麼？」/ Text / 必填
   - 題目：「你對工作環境的滿意度？」/ Rating / 必填
   - 題目：「哪些方面可以改善？」/ MultipleChoice，選項：溝通能力、專業技能、時間管理 / 必填
4. 點「建立模板」
5. **預期**：模板出現在列表，狀態 `Draft`

### T-TMPL-02 建立模板時 MultipleChoice 選項驗證

1. 新增模板，加一道 MultipleChoice 題
2. 選題型後確認：
   - 出現「選項（至少 2 個）」輸入區
   - 預設兩個空白輸入框
   - 可點「+ 新增選項」增加（無上限）
   - 超過 2 個選項時每個輸入框右側出現「×」刪除鈕；恰好 2 個時不顯示「×」
3. 不填選項直接送出 → 應顯示「多選題至少需要 2 個有效選項」錯誤
4. 只填 1 個選項，另一個空白 → 同上錯誤

### T-TMPL-03 發布模板

1. 找到 Draft 狀態的模板，點「發布」
2. 確認 Dialog 出現，確認後發布
3. **預期**：狀態變為 `Published`，上軌道第 2 格（模板已發布）打勾

### T-TMPL-04 Manager 新增自訂題目

> 切換至 Manager 帳號

1. 前往「評核模板」，點入已發布的模板
2. 點「+ 新增題目」
3. 新增一道 Text 題目
4. **預期**：題目出現在「自訂題目」區塊（藍底），基礎題目不受影響
5. 測試 MultipleChoice 題型：
   - 選 MultipleChoice 後出現選項輸入區
   - 填入至少 2 個選項後送出
   - 題目出現在自訂題目清單，選項正確顯示

### T-TMPL-05 HR 無法修改 Manager 自訂題目（設計決策）

1. 以 RegionalHR 登入，點入已有自訂題目的模板
2. **預期**：自訂題目區塊顯示題目但無刪除按鈕（HR 無法刪除 Manager 的題目）

### T-TMPL-06 Manager 刪除自訂題目

1. 以 Manager 登入，點入模板
2. 點自訂題目的「刪除」→ 確認 Dialog → 確認
3. **預期**：題目消失，其他題目不受影響

### T-TMPL-07 HR 無法刪除基礎題目

1. 模板詳情頁，基礎題目區塊
2. **預期**：無刪除按鈕；右側標示「已鎖定 — 由 HR 設定」

---

## 第四章：目標管理

### T-GOAL-01 員工建立目標

> 以 Employee 登入

1. 前往「我的目標」，點「+ 新增目標」
2. 填寫所有 SMART 欄位：
   - 描述、指標、目標數字、相關性、截止日期
3. 目標類型選「個人目標」
4. 週期欄位（如有進行中週期）：選擇一個週期
5. 點「建立目標」
6. **預期**：跳轉至目標詳情頁，狀態為 `Draft`

### T-GOAL-02 目標提交審核

1. 在目標詳情頁（Draft 狀態）
2. **預期**：顯示「草稿尚未提交」提示與「提交審核」按鈕
3. 點「提交審核」
4. **預期**：狀態變為 `PendingApproval`，按鈕消失

### T-GOAL-03 里程碑新增與管理

1. 在目標詳情頁，輸入里程碑名稱，點「新增」
2. 新增多個里程碑（至少 3 個）
3. 進度條應依完成比例動態更新
4. 拖拉左側 ⠿ 圖示調整順序 → **預期**：順序即時更新（hover 才顯示 drag handle）
5. 勾選一個里程碑 → 出現備注輸入框（可選填），確認後標記完成
6. 再次點勾 → 取消完成

### T-GOAL-04 Supervisor 審核目標

> 切換至 Supervisor 登入

1. 前往「我的團隊」，點入 Employee 的頁面
2. 找到 `PendingApproval` 狀態的目標，點入詳情
3. 點「核准目標」→ **預期**：狀態變為 `Approved`
4. 另一個目標點「退回」→ **預期**：狀態變為 `Draft`，員工可重新提交

### T-GOAL-05 目標逾期偵測

1. 建立一個截止日期為過去日期的目標
2. **預期**：詳情頁顯示逾期標示（紅色文字或 badge）

### T-GOAL-06 目標與週期關聯

1. 建立目標時，週期 dropdown 應只顯示 `GoalSetting` 或 `InProgress` 狀態的週期
2. 選擇一個週期，建立後目標詳情頁應顯示所屬週期名稱
3. 沒有活躍週期時 dropdown 不顯示

---

## 第五章：績效評核流程

> 前置：週期已推進至 `EmployeeReview`，系統自動建立 PerformanceReview 記錄

### T-REVIEW-01 自動建立評核確認

1. 以 Supervisor 登入 → 前往「團隊評核」
2. **預期**：列出所有符合模板條件的下屬評核，狀態為「待員工自評」
3. 員工前往「我的評核」→ **預期**：出現一筆待填寫的評核

### T-REVIEW-02 Employee 填寫自評

> 以 Employee 登入

1. 前往「我的評核」，點入評核
2. **版面確認**：頁面全寬，不被限制在半寬
3. **Breadcrumb 確認**：顯示「我的評核 > 週期名稱」
4. 逐題填寫：
   - Text 題：輸入文字
   - Rating 題：點選 1–5 其中一個數字
   - MultipleChoice 題：選擇其中一個選項
5. 點「儲存草稿」→ **預期**：頁面不跳轉，按鈕文字短暫變為「儲存中...」
6. 重新整理頁面 → **預期**：剛才的草稿資料保留
7. 點「送出自評」→ 確認 Dialog → 確認
8. **預期**：
   - 狀態 badge 變為「待主管評核」
   - 表單變為唯讀（欄位灰底）
   - 按鈕消失

### T-REVIEW-03 Supervisor 進入未填評核顯示 banner

> 切換至 Supervisor

1. 前往「團隊評核」，找到狀態「待員工自評」的評核，點入
2. **預期**：頁面頂部出現黃色 banner：「等待員工填寫自評，員工（XXX）尚未完成自評，送出後您才能進行主管評核」
3. **預期**：頁面不顯示任何評核填寫表單
4. Badge 在列表頁顯示「待員工自評」（非「待您填寫」）

### T-REVIEW-04 Supervisor 填寫主管評核

> 確保員工已送出自評

1. Supervisor 前往「團隊評核」，點入狀態「待主管評核」的評核
2. **版面確認**：頁面全寬
3. **Breadcrumb 確認**：顯示「團隊評核 > 週期名稱」
4. **不再顯示獨立員工自評區塊**：頁面頂部不應有獨立的「員工自評」section，員工答案只在下方左欄出現
5. **左右分欄確認**：
   - 左欄標題「員工自評（參考）」，灰底，完全唯讀
   - 右欄標題「主管評核意見」，白底，可輸入
   - 右欄每題都是 textarea（即使員工填的是 Rating 或 MultipleChoice）
6. 逐題在右欄填入評核意見
7. 捲到底部「整體評核說明」填入整體評語
8. **等第 tooltip 確認**：
   - 滑鼠 hover「ⓘ 等第說明」
   - 出現 tooltip，顯示 O/S+/S/S-/I/U 六個等第的中文名稱與說明
9. 點選一個等第（如 S）
   - 選取後該按鈕高亮
   - 下方出現「滿意：完全達成所有目標與期望」說明文字
   - 選 O 時出現「⚠ O（傑出）等第為極少數頂尖員工，請謹慎評定。」提示
   - 選 U 時出現「⚠ U（未達標）將進入 PMD 追蹤...」提示
10. 點「儲存草稿」確認不跳頁
11. 點「送出評核」→ 確認 Dialog → 確認
12. **預期**：評核狀態變為「待校準」或「待經理審核」

### T-REVIEW-05 Employee 查看已發布評核

> Manager 發布後（見下方 T-REVIEW-06）

1. Employee 前往「我的評核」，點入已發布評核
2. **結果 banner 確認**：
   - 綠色 banner 顯示等第（如大字 `S`）
   - 等第旁顯示中文名稱與說明（如「滿意 · 完全達成所有目標與期望」）
   - 右上角有「ⓘ 等第說明」tooltip
3. **主管評語區確認**：
   - banner 下方顯示主管填寫的整體評語
4. **主管逐題評語區確認**：
   - 員工自評下方出現「主管逐題評語」section
   - 每題顯示主管在右欄填寫的文字

---

## 第六章：校準與發布

### 前置：以 Manager 登入，週期推進至 Calibration

### T-CALIB-01 進入校準頁

1. 前往「團隊評核」
2. 找到 Calibration 週期，點「進入校準」
3. **預期**：進入 `/reviews/calibrate/[cycleId]`
4. 頁面顯示表格，列出所有待發布的評核

### T-CALIB-02 校準頁等第調整

1. 找到一筆評核，點擊等第選擇器中的按鈕（如 S+）
2. **預期**：
   - 按鈕立即高亮（Optimistic Update）
   - API 在背景儲存
   - 不整頁 reload

### T-CALIB-03 校準頁排名調整（Optimistic Update）

1. 找到一筆評核，在「排名」欄輸入數字（如 3）
2. 點頁面其他地方（onBlur 觸發）
3. **預期**：
   - 數字立即保留，不整頁 reload
   - 無 loading 轉圈或頁面閃爍
4. 輸入非數字或負數 → 不觸發 API（欄位自動復原）

### T-CALIB-04 並排比較

1. 校準頁點「並排比較 →」
2. **預期**：進入 `/reviews/compare/[cycleId]`
3. 評核以卡片水平並排，依等第排序（O 最左 → U 最右）
4. 每張卡片顯示：員工名稱、職稱、職等、等第 badge、狀態 badge、排名、主管評語
5. 點「查看完整評核 →」可跳至該評核詳情頁

### T-CALIB-05 一鍵發布所有評核

1. 回到校準頁，確認至少一筆評核已設好等第
2. 點「發布全部（N 份）」
3. 確認 Dialog 出現，點「確認發布」
4. **預期**：
   - 頁面 reload 後，已發布的評核狀態變為 Published
   - 待發布計數歸零
5. 未設等第的評核點「發布全部」→ 後端應處理（或前端 disabled）

### T-CALIB-06 Sidebar 說明文字（Manager）

1. 以 Manager 登入，查看左側 Sidebar
2. **預期**：
   - 「我的評核」下方有小字「自己的績效表單」
   - 「團隊評核」下方有小字「下屬的評核狀況」

---

## 第七章：申訴機制

### T-APPEAL-01 Employee 提出申訴

> 前提：評核狀態為 Published

1. Employee 前往「我的評核」，點入已發布的評核
2. 捲到結果 banner 底部，點「對結果有異議？提出申訴 →」
3. **預期**：展開申訴原因輸入區
4. 少於 10 字送出 → 按鈕 disabled，無法送出
5. 填入至少 10 字的原因，點「確認提交申訴」
6. **預期**：
   - 評核狀態變為「申訴審核中」（Appealed）
   - banner 顏色變為紅色
   - 顯示「您的申訴已送出，等待上級經理審閱」

### T-APPEAL-02 Supervisor 無法查看申訴

1. 以 Supervisor 登入，嘗試直接訪問 `/appeals/[id]`（該申訴 ID）
2. **預期**：回傳 403 或顯示「找不到申訴或無權限查看」

### T-APPEAL-03 Manager 查看申訴列表

1. 以 Manager 登入，前往「申訴管理」
2. **預期**：列出所有收到的申訴，顯示員工姓名、週期、申訴狀態
3. 點入一筆申訴

### T-APPEAL-04 Manager 回覆申訴（含調整等第）

1. 在申訴詳情頁，確認顯示：
   - 員工申訴原因（紅底區塊）
   - 原始評核等第
2. **等第選擇器確認**：
   - 「回覆申訴」表單頂部顯示「調整等第（選填）」
   - 顯示 O/S+/S/S-/I/U 六個按鈕
   - 括號內顯示目前等第（如「不選則維持原等第 S」）
   - 點選一個等第 → 按鈕高亮；再次點擊 → 取消選擇
3. 不調整等第，只填回覆文字（少於 5 字）→ 按鈕 disabled
4. 填入至少 5 字的回覆，選擇新等第（如 S+）
5. 點「送出回覆並解決申訴」
6. **預期**：
   - 申訴狀態變為 Resolved
   - 頁面顯示「經理回覆」藍底區塊，內容為剛才填入的文字
   - 評核等第被更新為 S+（重新查看評核頁面確認）

### T-APPEAL-05 Employee 查看已解決申訴

1. Employee 前往已申訴的評核
2. **預期**：
   - 評核狀態從「申訴審核中」恢復為 Published
   - 等第顯示 Manager 調整後的新等第
   - 有「返回評核頁」連結

---

## 第八章：Dashboard

### T-DASH-01 Employee Dashboard

1. 以 Employee 登入，前往「備表板」
2. **預期顯示**：
   - 待填寫的評核數量
   - 進行中的目標（含完成進度）
   - 年度週期 Gantt 圖（1–12 月）

### T-DASH-02 Supervisor Dashboard

1. 以 Supervisor 登入
2. **預期顯示**：
   - 待評核的下屬數量
   - 下屬目標審核清單
   - Gantt 圖

### T-DASH-03 Manager Dashboard（等第分布圖）

1. 以 Manager 登入
2. **預期顯示**：
   - 「等第分布」橫條圖，顯示 O/S+/S/S-/I/U 各等第的員工人數與比例
   - 總評核數、待發布數
   - 已發布評核才計入分布（無等第的不算）

### T-DASH-04 RegionalHR / Admin Dashboard（全局等第分布）

1. 以 RegionalHR 登入
2. **預期顯示**：「全局等第分布」橫條圖（跨所有 Supervisor/Manager）

### T-DASH-05 Gantt 圖確認

1. 任一角色登入，查看 Dashboard Gantt 圖
2. **預期**：
   - 顯示當年度 1–12 月時間軸
   - 目標設定期顯示藍色區間
   - 評核期顯示紫色區間
   - 今日有琥珀色標線

---

## 第九章：團隊管理

### T-TEAM-01 Supervisor 查看直屬員工列表

1. 以 Supervisor 登入，前往「我的團隊」
2. **預期**：列出所有直屬員工（DataTable 格式）

### T-TEAM-02 Manager 查看階層分組視圖

1. 以 Manager 登入，前往「我的團隊」
2. **預期**：
   - 依 Supervisor 分組顯示員工
   - 直屬員工（無 Supervisor）單獨顯示
   - 顯示各 Supervisor 姓名作為分組標題

### T-TEAM-03 員工詳情頁

1. 點入任一員工
2. **預期顯示**：
   - 基本資料（職稱、職等、地區）
   - 目標列表（可點入詳情）
   - 評核歷史（可點入評核）

---

## 第十章：完整端對端流程測試（E2E）

> 此章節模擬一個完整週期從頭到尾，建議在乾淨的測試環境執行。

### 流程總覽

```
HR 建立週期
  ↓
HR 建立並發布模板
  ↓
Manager 新增自訂問題
  ↓
HR 推進至 EmployeeReview（系統自動建立評核）
  ↓
Employee 填寫自評並送出
  ↓
Supervisor 填寫主管評核並送出
  ↓
HR 推進至 Calibration
  ↓
Manager 校準等第與排名
  ↓
Manager 發布結果
  ↓
Employee 查看結果 → 提出申訴
  ↓
Manager 回覆申訴（調整等第）
  ↓
Employee 確認最終結果
```

### E2E-01 步驟執行

**Step 1：RegionalHR 建立週期**
- 建立名稱「E2E 測試週期」，Quarterly，Taiwan
- 確認 Stepper 在 GoalSetting

**Step 2：RegionalHR 推進至 InProgress**
- 點「進入執行中 →」→ 確認 Dialog（注意【】格式）→ 確認

**Step 3：RegionalHR 建立並發布模板**
- 建立含 Text + Rating + MultipleChoice 三題的模板
- 適用職等/職稱匹配測試員工
- 發布模板 → 上軌道第 2 格打勾

**Step 4：Manager 新增自訂題目**
- 進入模板詳情，新增 1 道 Text 自訂題
- 上軌道第 3 格「經理補充問卷」打勾

**Step 5：RegionalHR 推進至 EmployeeReview**
- 點「開始員工自評 →」
- 確認 Dialog 顯示 Manager 完成狀態（綠色 ✓）
- 確認後推進
- 確認系統自動建立 PerformanceReview 記錄

**Step 6：Employee 填寫自評並送出**
- 登入 Employee，確認「我的評核」有新記錄
- 填寫並儲存草稿，確認重整後資料保留
- 送出自評

**Step 7：Supervisor 填寫主管評核並送出**
- 登入 Supervisor，確認左右分欄顯示
- 填寫右欄所有題目，選擇等第
- 送出評核

**Step 8：RegionalHR 推進至 Calibration**

**Step 9：Manager 校準並發布**
- 調整等第與排名（確認 Optimistic Update）
- 查看並排比較頁
- 發布全部

**Step 10：Employee 確認結果並申訴**
- 查看發布結果（確認等第說明、主管逐題評語）
- 提出申訴

**Step 11：Manager 回覆申訴**
- 調整等第，填入回覆
- 解決申訴

**Step 12：Employee 確認最終結果**
- 確認等第已更新

---

## 附錄：已知限制（不在測試範圍內）

| 項目 | 說明 |
|------|------|
| Audit Log CRUD | 目前只記錄登入/登出，CRUD 操作未記錄 |
| 申訴結果通知 | 無推播通知，需員工主動查看 |
| Session 閒置登出 | 目前固定 8 小時，未實作 30 分鐘閒置偵測 |
| MFA / SSO | 開發階段跳過 |
| 自訂題目跨週期複用 | 每季需手動重新新增（待做 T1） |

---

## 附錄：測試帳號快速切換建議

建議使用兩個瀏覽器或一個無痕視窗同時開啟，方便在不同角色間切換：

- **Chrome 正常視窗**：Supervisor / Employee
- **Chrome 無痕**：Manager / RegionalHR

---

*最後更新：2026-05-07*
